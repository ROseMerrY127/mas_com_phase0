# 通信层路由机制 —— 规范与验证预案(最终版)

> 本文档定义的**不是一个固定的、随插随用的路由器插件**,而是一套**抽象机制**:它规定了"多智能体系统的通信层路由机制应由哪些部分组成、各部分的接口是什么、如何训练出一个实例",并允许后续研究者与使用者针对自己的任务类型**填空、微调,得到他们各自的路由器实例**。
>
> 本工作的性质是**提出一个范式**,而非交付一个产品。数学推理任务在本文档中的角色是**第一个参考实现(reference implementation)**,用于证明机制可行;它不是工作的主体。
>
> **成功标准**:插件的成功标准是"它在某任务上 work";机制的成功标准是"**不同的人按它填空,都能得到 work 的实例**"。

---

## 0. 文档定位:协议,而非模型

交付三样东西,按重要性排序:

1. **接口规范**(§3):机制层与适配层之间的契约。核心贡献 —— 别人按此填空即可接入新任务。
2. **训练协议**(§4):任务无关 recipe,输入"你的领域奖励 + 你的环境",输出"训好的路由器实例"。
3. **参考实现 + 验证**(§5–§8):数学域填好一个实例并验证,再在第二域复现,证明机制可复用。

> **一句话定位**:我提出一个机制 + 一个数学参考实现来证明它可行,而非"我做了一个数学路由器"。

---

## 1. 机制定位与核心主张

### 1.1 机制是什么

挂在多智能体系统通信层之上的**隐式路由机制**。不参与任务完成,系统拓扑不变,但所有通信必须经过它(类比电力系统变电站)。对每条边上的消息做三类决策:

- **权重分配** —— 决定每条通路的重要性
- **处理/压缩判断** —— 决定是否压缩、压到什么档位
- **转发 / 阻断** —— 决定消息是否放行

它有一个奖励模型,根据该模型反馈做出决策。

### 1.2 核心主张:机制可泛化,实例不可泛化

把路由机制切成两层 —— **这个切分本身就是本工作的主要贡献**:

- **任务无关的机制层(可泛化,跨任务复用)**:边特征化、决策头、动作空间、训练算法。后续研究者**不修改**。
- **任务耦合的适配层(每域填空)**:领域奖励来源 + 压缩器实现。后续研究者**只实现**这一层。

> **结论**:"适用于任意拓扑" ≠ "对任意拓扑有用"。机制价值与拓扑冗余度成正比 —— 只在有并行、有冗余、有多条竞争通路的图上有意义。单边流水线上退化成过滤器。

### 1.3 抽象的适用边界(防止抽象过度,保证可证伪)

> **适用于**:拓扑固定、存在并行/冗余通路、每个域能提供步级或结果级**可归一化奖励**的多智能体系统。
>
> **不适用于**:单边流水线(无权重可分配)、无法定义任何过程或结果奖励的任务(核心信号缺失)。

---

## 2. 与传统角色的关系:仅一处真重叠

| 传统角色 | 是否重叠 | 说明 |
|---|---|---|
| Orchestrator | 否 | Orchestrator 路由**任务**(语义);本机制路由**已有边上的消息**(非语义信号控制) |
| Critic / Verifier | 否(互补) | Critic 评**输出正确性**;本机制 RM 评**通信价值**。RM 应**消费** Critic/PRM 输出作训练信号 |
| **Memory / Context Manager** | **是** | 唯一真冲突。两者都做压缩。**边界**:Memory 管长期存储与检索,本机制管**在途消息**的实时压缩 |

---

## 3. 接口规范(核心贡献)

接入新任务**只需实现本节三个契约**,机制层与训练协议保持不变。

### 3.1 接口一 —— 输入契约:适配层提供什么

机制层吃四类特征,**只有第一类任务相关**,需适配层提供;其余三类机制层自算。

| 特征类 | 需适配层提供? | 内容 | 为何任务无关 |
|---|---|---|---|
| **① 通路历史回报** | **是(唯一要填)** | 每条边归一化平均贡献度 | 来自领域奖励,聚合归一化后形状跨域一致 |
| ② 消息内生不确定性 | 否 | 生成方 token logprob、熵、自洽性 | 模型内部信号,与任务内容无关 |
| ③ 消息间冗余度 | 否 | 嵌入相似度 | 纯几何 |
| ④ 结构位置特征 | 否 | 第几轮、距输出几步、扇入扇出度 | 纯拓扑 |

**适配层唯一要实现的函数**:

```
domain_reward(message, context) -> 归一化标量 ∈ [0, 1]
```

数学填 PRM 步分;代码填执行通过率;逻辑填形式验证。机制层**永不直接看 PRM 分**,只看"归一化历史贡献度"。

**设计纪律**:输入特征向量中**不允许出现任何任务专用原始信号**。每加一个特征问"它在代码域有对应物吗",没有就不准进。

### 3.2 接口二 —— 输出契约:动作空间

动作空间必须任务无关。**关键洞察:权重 → capacity 把三个动作统一成一个连续标量**(见 §3.4)。

压缩动作的两层切分:

| 层 | 职责 | 归属 |
|---|---|---|
| "压多少" | 选择压缩档位 / 目标 token 数 | **机制层(任务无关)** |
| "怎么压" | 具体压缩器实现 | **适配层(任务专用,填空插槽)** |

### 3.3 接口三 —— 过程契约:训练协议调用

```
train_router(domain_reward, domain_compressor, environment) -> trained_router
```

别人提供自己的三个空,即可跑出实例。recipe 本身不依赖任务。

### 3.4 权重如何影响决策:capacity 方案(已定)

> 决策:**权重映射为 capacity(链路 token 预算),而非作为置信度标签塞进消息**。

**两个候选方案的本质差异 —— 谁消费权重**:

| 方案 | 本质 | 权重消费者 | 是否任务无关 | token 账 |
|---|---|---|---|---|
| **① capacity(已选)** | 带宽控制 | 路由器/信道(闭环在通信层内) | **是** | 直连节省 |
| ② 置信度注入 | 元数据标注 | 下游 agent(需读懂并改变行为) | **否**(解读高度任务/prompt 相关) | 净增(消息原样传+多塞元数据) |

**为何选 capacity**:
1. 闭环在通信层内部,不依赖下游 agent 配合 → 任务无关,符合机制定位。
2. 直连 token 账(权重低 → capacity 小 → 压缩 → 省 token)。
3. **三个动作塌缩成一个连续标量**:capacity 满 → 全量转发;适中 → 压缩;为零 → 阻断。动作空间一下子干净了。

**为何不选置信度注入**:它把"任务无关的控制问题"变成"任务相关的理解问题"(同一个 0.3 在数学/代码 Solver 眼里意义不同,又要为每域调 prompt),且机制成败押在"下游 agent 会不会读懂一个数字"上 —— 违反任务无关与即插即用目标。

**关键实现陷阱:不要用天真线性映射 `capacity = weight × 满容量`**。原因:权重(贡献度)与"该传多少 token"非线性、甚至非单调。
- 反例 A:高质量但冗长的推理 —— 高权重,但很多 token 冗余,不需要大 capacity(可无损压缩)。
- 反例 B:简短但关键的纠错提示 —— token 本就少,给再大 capacity 也没用。

**正确设计:权重与 capacity 解耦**:
- **权重(贡献度)**:RM 输出,用于**排序与预算分配**。
- **capacity(token 预算)**:由权重 + 消息可压缩性(§3.1 第③类冗余度特征)**共同**决定。
- 映射逻辑:**在固定总 token 预算下,按权重把预算分配给各边,但每条边实际 capacity 还受其可压缩性调节** —— 这是预算约束下的分配问题,而非逐边独立线性缩放。

**置信度注入的处置**:降为**可选附属信号**。可在保留的消息里附置信度标签,但**机制有效性不依赖下游是否理会它** —— 即使所有下游 agent 忽略标签,靠 capacity 控制依然 work。锦上添花,不是承重墙。

### 3.5 压缩器选型:判别式抽取式(已定)

> 决策:**默认压缩器用判别式 token 分类器(LLMLingua-2 类抽取式);规则删除作数学域轻量基线;LM 改写基本放弃**。

**三种候选**:

| 方案 | 类别 | 评价 |
|---|---|---|
| 规则删结构化文本(原预案1) | 抽取式(规则) | 极便宜,但不知删的部分重不重要 → 数学域轻量基线 |
| **LLMLingua-2 类(推荐默认)** | 抽取式(判别式 token 分类) | 实证优于抽象式、用小编码器非生成式、任务无关 |
| LM 改写(原预案2) | 抽象式(生成式) | **更贵且通常更差** → 基本放弃,仅作消融对照 |

**关键实证结论(来自压缩文献)**:
- 抽取式往往**优于**抽象式:有研究显示抽取式在 4.5x 压缩下反而提升准确率(过滤噪声),同等压缩率抽象式则掉分。
- LLMLingua 系列用**小模型**算 token 困惑度/信息量来删低信息 token,可达高压缩率且性能损失小;LLMLingua-2 是 BERT 级编码器做 token 二分类(保留/删除),判别式、任务无关、域外表现好。
- LM 改写(抽象式)本身要跑一次生成 → 正是要避免的生成式开销,还可能掉准确率。对 token 敏感的本机制尤其致命。

**反直觉收益:压缩可能不是 trade-off 而是双赢**。文献显示 LLM 表现会随提示噪声增加而下降,故压缩(去噪)在某些情况下**提升**准确率。应在实验中专门验证"压缩后准确率是否不降反升",若成立是有力论点。

**架构协同**:LLMLingua 的"按信息熵/困惑度删 token"与机制层算的"消息内生不确定性"(§3.1 第②类)**同源**(都是 token 级信息量度量)。机制层算特征的中间量可直接复用给压缩器,省一次计算。

---

## 4. 训练协议(任务无关 recipe)

> 本质难点:路由决策会改变轨迹 → 反事实/RL 问题,非监督学习。

### Phase 0 —— 透传日志
路由器置 identity 模式(全转发、等权、不压缩)。记录每条边完整信息 + 每步领域奖励 + 最终结果奖励。→ 基础语料。

### Phase 1 —— 反事实对比数据(最关键)
同一输入用扰动后路由重跑,测 Δ步级/Δ结果奖励,得对比样本对。三种做法:
- **Leave-one-message-out**:删消息重跑,Δ即因果贡献度。金标准,贵 → 仅小子集作校准。
- **压缩消融**:全量 vs 压缩版重跑,下游奖励守住→可压,掉→不可压。直接给压缩决策打标签。
- **噪声注入**:塞冗余,确认机制学会压掉。

大头靠 Phase 0 密集步级奖励做**代理标签**。

### Phase 2 —— 在线 RL
RM 冷启动后,路由器实际决策、on-policy 采样、用奖励更新策略与 RM。

### 奖励模型设计要点

**多目标奖励 + 防 reward hacking**:
```
R = α · 质量(步级奖励 + 结果奖励) + β · token 节省
```
> 天真奖励 token 节省 → 退化最优解(全压全砍)。**解法**:压缩奖励**有条件** —— 仅当下游质量保持阈值之上才给 token 节省奖励。无损压缩被奖励,有损压缩被质量项惩罚。

**两阶段训练**:① 监督预训练(Phase 0+1,学"决策→期望回报",摊销的反事实估计)→ ② 在线精调(Phase 2 on-policy)。不要一上来在线 RL(太不稳定,分不清是机制问题还是 RL 没调好)。

### token 节省的完整账本
```
Δtoken = 路由器自身开销 − 压缩节省 − 阻断节省 − 去冗余节省 − 减少返工节省
```
压缩只是四项节省之一,即使压缩为零,后三项仍可能让总账为负。

> **设计约束**:路由器自身开销必须 < 它创造的总节省。故路由器**做成判别式打分器(小 RM + 轻量决策头),非生成式 LLM**。复杂策略来自 RL 学到的非线性映射,非大模型生成;抽象式压缩等生成动作仅在决策核心选择它的少数边上按需触发。

---

## 5. 参考实现:数学推理域

> 数学是**第一个参考实现**,非工作主体。选它:每步可 PRM 打分(密集过程信号)、数据多、答案可 `math-verify` 自动验证、一道题 = 几十个边-决策样本。

按 §3 填空:

| 接口 | 数学域填法 |
|---|---|
| `domain_reward` | PRM 步分(Qwen2.5-Math-PRM / Math-Shepherd PRM),归一化 [0,1] |
| `domain_compressor`(默认) | LLMLingua-2 类判别式 token 分类器 |
| `domain_compressor`(基线) | 规则抽取:保留等式/不等式链,丢自然语言解释 |
| 结果奖励 | `math-verify` 验证最终答案 |

参考拓扑:2 个并行 Solver(A=直接 CoT、B=分步分解)+ Step Verifier + Planner + Final Judge。**必须有并行路径**。

---

## 6. 验证计划:五阶段 + 四检查点 + 主验证

> 原则:便宜、能一票否决的实验放最前。
> **重心**:本工作是"机制"非"插件",单域(数学)work 只是**必要条件**;主结论是 **Stage 5 跨域复现**。

### Stage 0 —— 搭建(数天)
固定:参考系统、GSM8K 子集迭代、MATH-500 评测(训练集与评测集**严格去重**)、评分器。路由器先 identity。
**检查点 0**:MAS(no-router) > 单 agent CoT?否 → 先修系统。

### Stage 1 —— Oracle 路由器(数天,最关键)
用真实步级/结果奖励 ground truth 作奖励决策(作弊,仅验证机制)。
**精度边界**:步级奖励高 ≠ 最终最优(信用分配非局部)→ 此 Oracle 给"RM 完美模仿步级奖励时的上界",对否决判据已足够。
**科学核心:对照 oracle-best-of-N**(每 Solver 各跑完整解答,结果奖励挑最优)。
> **真问题**:oracle-router 能否显著打过 oracle-best-of-N?打不过 → 边级路由不过是采样挑最优,不需要机制;打得过 → 子解粒度中间路由带来重组/纠偏,这是立身之本。

**实验臂(相同总采样预算)**:① no-router ② oracle-best-of-N ③ oracle-router(步级 only,不许偷看结果奖励)④ oracle-router + 压缩。
**新增对照(权重方案,验证 §3.4)**:纯 capacity vs 纯置信度注入 vs 二者结合 —— 比准确率与 token 总账。预测:纯 capacity 在 token 轴胜出,置信度注入准确率微增但 token 净增且随 agent/prompt 不稳定。

**检查点 1(机制有效性)**:

| 准确率 \ 成本 | 不优于 best-of-N | 显著优于 best-of-N |
|---|---|---|
| **成本显著降低** | 仅效率价值 → 重定位为压缩层 | **双重价值 ✓ 全速推进** |
| **成本无改善** | **机制不成立 → 否决,停止/重构动作空间** | 质量价值成立 → 继续,排查压缩为何没省 |

> 若用完美信息都做不到 → **在此停止,不进数据采集**。

### Stage 2 —— 数据采集(数周)
仅过检查点 1 后。Phase 0 + Phase 1。leave-one-out 仅小子集作校准。

### Stage 3 —— RM 训练(1–2 周)
监督预训练 → 在线精调。
**检查点 2(RM 质量)**:RM 预测与实测 Δ步级奖励 Spearman 相关?低 → 回 Stage 2 扩样本。

### Stage 4 —— 学习版 + 消融(1–2 周)
- **A1** 关动态权重(等权)→ 隔离权重分配
- **A2** 压缩器对比 → **无压缩 / 规则删除 / LLMLingua-2 / LM 改写** 四臂,比 token 节省 × 准确率(看是否因去噪上升)× 压缩器开销
- **A3** 单 vs 双 Solver → 隔离路径选择
- **A4** learned vs **random** → 零假设对照
- **A5** learned vs 静态一次性剪枝(AgentPrune 式)→ 证明动态路由优于静态剪枝

**检查点 3**:learned 明显优于 random 且接近 oracle?不达标 → 精调奖励定义。

### Stage 5 —— 跨域复现(★ 主验证)
> 升级为整个工作的主结论,验证 §1.2 核心主张。

**做法**:**冻结机制层 + 训练 recipe**,只重写适配层三个空(`domain_reward` → 执行通过率/形式验证;`domain_compressor` → 对应域压缩器),在**至少一个差异足够大的第二域**(代码/逻辑)跑完整流程。

**主验证指标 —— 跨域一致性(非数学绝对性能)**:同一机制第二域能否优于基线?各域相对自身 oracle 上界掉多少?需多少额外微调(理想:开箱可用)?

**诚实的风险**:任务无关特征可能不足以支撑足够好决策。若如此,Stage 1/4 会见判别式明显打不过吃原始 PRM 的版本 → 真实的"泛化弱机制 vs 专用强机制"取舍,应显式测出,非预设。

---

## 7. 基线与指标汇总

**基线(弱→强)**:单 agent CoT → no-router(全广播)→ random router(零假设)→ static pruning(AgentPrune 式)→ oracle router(上界)→ learned router(本机制实例)

**核心指标**:准确率(Pass@1,多 seed 均值±标准差)、成本(每题 token,**含路由器自身开销**)、效率(准确率/千 token 或 Pareto 前沿)、RM 质量(预测与 Δ步级奖励相关性)、**跨域一致性(机制论文核心)**。

**统计纪律**:多 seed、报告均值方差、显著性检验。机制价值两形态(更高准确率 / 同准确率更低成本),**必须同时测准确率和 token**。

---

## 8. 检查点速查(失败即知道退到哪)

| 检查点 | 判据 | 失败动作 |
|---|---|---|
| **0 · 前提** | MAS > 单 agent CoT | 先优化基础系统 |
| **1 · 机制有效性** | oracle-router > no-router 且 > oracle-best-of-N | **停止/重构动作空间,不进数据采集** |
| **2 · RM 质量** | RM 预测与实测 Δ步级奖励相关 | 回 Stage 2 扩反事实样本 |
| **3 · 学习版有效性** | learned > random 且接近 oracle | 精调奖励定义 |
| **★ 主验证 · 跨域复现** | 冻结机制层换适配层,第二域仍优于基线 | 泛化主张不成立 → 退回单域插件,重述贡献 |

---

## 9. 关键设计决策速查(本项目已拍板的结论)

| 议题 | 决策 | 理由速记 |
|---|---|---|
| 路由器实现形态 | **判别式打分器(小 RM + 轻量决策头)** | 生成式 LLM 每边开销大,token 账难算平 |
| 复杂策略来源 | **RL 学到的非线性映射**,非大模型生成 | 决策本身低维;语义理解复用已有 Verifier/PRM |
| 泛化信号来源 | **复用"信号的形状"**(4 类任务无关特征),非语义内容 | 任务专用信号(PRM)经适配层归一化为统一贡献度 |
| 权重如何影响决策 | **映射为 capacity**(§3.4),非置信度标签 | 闭环通信层内、任务无关、三动作塌缩为连续标量 |
| capacity 映射方式 | **权重与 capacity 解耦**,预算约束下分配 | 权重×满容量的线性映射非单调,有反例 |
| 置信度注入 | **降为可选附属**,机制不依赖它 | 解读任务相关,不能做承重墙 |
| 压缩器默认 | **判别式抽取(LLMLingua-2 类)** | 抽取式实证优于抽象式、小编码器、任务无关 |
| 压缩器基线 | 规则删除(数学域) | 极便宜,参考实现求简洁 |
| LM 改写压缩 | **放弃**(仅作消融对照) | 更贵且通常更差,生成式开销 |

---

## 附:相关工作锚点

- **AgentPrune**:可微边掩码,一次性剪枝时空通信图 —— 对照组 A5(优化结构,query 级一次性)
- **G-Designer**:RL 学习任务专属稀疏通信图
- **AgentDropout**:自适应丢弃低效用边降 token
- **GPTSwarm**:agent 作为计算图节点优化连接
- **LLMLingua / LLMLingua-2(Microsoft)**:prompt 压缩 SOTA;基础版用小模型按困惑度删 token,-2 版 BERT 级编码器做任务无关 token 分类 —— 本机制默认压缩器候选
- **Prompt Compression Survey(NAACL 2025)**:hard prompt(抽取/改写)vs soft prompt(压成特殊 token)分类框架

> **本机制与上述关键区别**:它们优化**结构**(query 级、一次性)或仅做**通用压缩**;本工作贡献的是一套**可被他人填空复用的运行时边级路由机制 + 接口规范**,压缩是一等公民动作,权重作为 capacity 统一动作空间,以跨域复现为主验证。
